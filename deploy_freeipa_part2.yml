- hosts: freeipa
  become: yes
  become_method: sudo
  tasks:
    - import_tasks: roles/freeipa/setup_freeipa_service_users.yml
    - import_tasks: roles/freeipa/setup_freeipa_users.yml
      vars:
        rockyou: "{{ lookup('file', 'conf/freeipa/rockyou.txt').split('\n') }}"